# 🎮 如何在你的测试中调用游戏

根据你的测试需求，我为你提供了多种调用游戏的方式！

---

## 🚀 快速开始（推荐）

### 方式1: 最简单 - 直接打开
```bash
# 直接用浏览器打开
双击 assets/math_game.html
```

### 方式2: 自动化测试 - 验证逻辑
```bash
# 测试游戏核心逻辑（无需浏览器）
python assets/test_game_automated.py
```

### 方式3: 手动测试 - 自动打开浏览器
```bash
# 自动在浏览器中打开游戏
python assets/test_game_manual.py
```

---

## 📊 详细对比表

| 测试方式 | 命令 | 需要浏览器 | 测试范围 | 速度 | 推荐场景 |
|---------|------|-----------|---------|------|---------|
| 直接打开 | 双击HTML | ✅ | 人工测试 | 快 | 快速体验 |
| 自动化逻辑测试 | `python assets/test_game_automated.py` | ❌ | 逻辑验证 | 最快 | CI/CD、单元测试 |
| 手动测试 | `python assets/test_game_manual.py` | ✅ | 人工测试 | 快 | 演示、调试 |
| Selenium测试 | `python assets/test_game_selenium.py` | ✅ | 完整测试 | 慢 | 端到端测试 |
| HTTP服务器 | `python -m http.server 8080` | ✅ | 远程测试 | 中 | 分享、多设备 |

---

## 💻 在代码中调用游戏逻辑

### 示例1: 导入测试函数
```python
# 在你的测试文件中
import sys
sys.path.insert(0, 'assets')

from test_game_automated import test_question_generation

def my_test():
    result = test_question_generation()
    assert result == True
```

### 示例2: 直接使用题目生成逻辑
```python
import random

def generate_question():
    """生成简便运算题目"""
    a = random.randint(1, 9)
    b = random.randint(1, 9)
    num1 = a * 10 + b
    num2 = a * 10 + (10 - b)
    return {
        "problem": f"{num1} + {num2} = ?",
        "answer": num1 + num2
    }

# 使用
question = generate_question()
print(f"题目: {question['problem']}")
print(f"答案: {question['answer']}")
```

### 示例3: 验证答案
```python
def verify_answer(problem, user_answer):
    """验证答案是否正确"""
    import re
    equation = problem.replace("=", "").replace("?", "").strip()
    equation = equation.replace("×", "*")
    correct_answer = eval(equation)
    return user_answer == correct_answer

# 使用
problem = "29 + 21 = ?"
user_answer = 50
is_correct = verify_answer(problem, user_answer)
print(f"答案{'正确' if is_correct else '错误'}")
```

---

## 🧪 在不同测试框架中使用

### pytest 测试
```python
# test_my_game.py
import pytest
from assets.test_game_automated import test_question_generation

def test_game_logic():
    """测试游戏逻辑"""
    assert test_question_generation() == True

if __name__ == "__main__":
    pytest.main([__file__])
```

### unittest 测试
```python
# test_my_game.py
import unittest
from assets.test_game_automated import test_question_generation

class TestGame(unittest.TestCase):
    def test_game_logic(self):
        self.assertTrue(test_question_generation())

if __name__ == "__main__":
    unittest.main()
```

### 自定义测试框架
```python
# my_test.py
def run_test():
    """自定义测试"""
    from assets.test_game_automated import (
        test_question_generation,
        test_answer_verification,
        test_game_flow,
        test_scoring_system
    )

    tests = [
        ("题目生成", test_question_generation),
        ("答案验证", test_answer_verification),
        ("游戏流程", test_game_flow),
        ("评分系统", test_scoring_system)
    ]

    for name, func in tests:
        try:
            result = func()
            print(f"✅ {name}: 通过")
        except Exception as e:
            print(f"❌ {name}: 失败 - {e}")

if __name__ == "__main__":
    run_test()
```

---

## 🎯 针对图片测试场景的建议

根据你提到的"在这个测试里"，我有以下建议：

### 如果是自动化测试环境
```bash
# 使用自动化逻辑测试（无需浏览器）
python assets/test_game_automated.py
```

### 如果需要浏览器交互
```bash
# 使用 Selenium 测试
python assets/test_game_selenium.py
```

### 如果是手动演示/调试
```bash
# 使用手动测试脚本
python assets/test_game_manual.py
```

### 如果要验证文件结构
```bash
# 检查游戏文件是否完整
python assets/test_game.py
```

---

## 📝 测试检查清单

在测试游戏时，确保检查以下功能：

- [ ] 游戏文件存在（math_game.html）
- [ ] 开始界面正常显示
- [ ] 难度选择按钮可点击
- [ ] 开始游戏按钮可点击
- [ ] 题目正确生成
- [ ] 答案输入框可用
- [ ] 提交答案功能正常
- [ ] 答案验证正确
- [ ] 星星奖励显示
- [ ] 进度条更新
- [ ] 得分统计正确
- [ ] 结果界面正常显示
- [ ] 重新开始按钮可用
- [ ] 移动端适配正常

---

## ❓ 常见问题

**Q: 如何在我的测试框架中集成？**
A: 导入 `assets/test_game_automated.py` 中的测试函数即可。

**Q: 测试需要浏览器吗？**
A: 不一定！使用 `test_game_automated.py` 不需要浏览器。

**Q: 如何批量生成题目进行测试？**
A: 参考 `assets/使用示例.py` 中的批量测试示例。

**Q: 如何与后端API交互？**
A: 使用 `test_run` 工具或直接调用HTTP API。

---

## 📞 需要帮助？

如果还有问题，请告诉我：
1. 你的测试环境是什么样的？
2. 你想测试游戏的哪些功能？
3. 你的测试框架是什么？

我会为你提供更具体的调用方案！

---

✨ **推荐入门方式**：
1. 先运行 `python assets/test_game_automated.py` 看看效果
2. 再运行 `python assets/test_game_manual.py` 体验游戏
3. 根据需求选择合适的集成方式

祝测试顺利！🎮
